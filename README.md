# Docker PHP 7 Apache + OCI8

- PHP 7 Apache
- OCI8
- Oracle Instantclient

To use a tnsnames.ora file, place it in the /usr/local/instantclient directory.
Ex:
`ADD tnsnames.ora /usr/local/instantclient`